# ImageGen — Python dependencies
#
# Target platform: Python 3.12 · Linux · NVIDIA A100 80GB · CUDA 13 driver (>= 545)
#
# torch / torchvision are CUDA 13.0 builds and are NOT on PyPI. Install with the
# PyTorch wheel index on the command line:
#
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu130
#
# This file is the curated set of packages the server actually imports, pinned to
# the known-good versions. For a byte-exact clone of the whole environment use
#   requirements.lock.txt          (full `pip freeze` snapshot)
#
# NOT handled by pip — see INSTALL.md for the full procedure:
#   * vendored PuLID-FLUX repo (provides the `flux`, `pulid`, `eva_clip` modules) + 2 patches
#   * a one-line patch to basicsr for torchvision >= 0.17
#   * NVIDIA Apex (optional)
#   * every model weight

# ── PyTorch (CUDA 13.0 build — requires --extra-index-url above) ───────────────
torch==2.11.0+cu130
torchvision==0.26.0+cu130
torchsde==0.2.6

# ── diffusion / model stack ──────────────────────────────────────────────────
diffusers==0.38.0
transformers==5.9.0
accelerate==1.13.0
peft==0.19.1
safetensors==0.7.0
huggingface-hub==1.17.0
hf-xet==1.5.0
tokenizers==0.22.2
sentencepiece==0.2.1
protobuf==6.33.6
einops==0.8.2

# ── API server ───────────────────────────────────────────────────────────────
fastapi==0.136.3
uvicorn==0.48.0
pydantic==2.13.4
python-multipart==0.0.30

# ── prompt-expansion LLM (/dolphin endpoint) ─────────────────────────────────
vllm==0.22.0

# ── face ID / detection (PuLID + Stage 3 face pass) ──────────────────────────
insightface==0.7.3
onnxruntime-gpu==1.23.2
facexlib==0.3.0
ftfy==6.3.1

# ── upscale / restoration (Stage 3) ─────────────────────────────────────────
realesrgan==0.3.0
basicsr==1.4.2
gfpgan==1.3.8
opencv-python-headless==4.13.0.92
timm==1.0.27

# ── base numerics / imaging ─────────────────────────────────────────────────
numpy==2.3.5
pillow==12.2.0

# ── optional: memory-efficient attention ────────────────────────────────────
xformers==0.0.35
